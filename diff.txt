diff --git a/include/state.h b/include/state.h
index 6dd90c1..bfcf177 100644
--- a/include/state.h
+++ b/include/state.h
@@ -1,6 +1,8 @@
 #pragma once
 
+#include "config.h"
+
 enum class State { SAFE, ALERT, DANGER };
 
-State computeState(float level, float rate, float humidity);
+State computeState(float level, float rate, float humidity, Config cfg);
 const char *stateToString(State s);
diff --git a/include/ultrasonic.h b/include/ultrasonic.h
index d29c403..a5a346c 100644
--- a/include/ultrasonic.h
+++ b/include/ultrasonic.h
@@ -4,4 +4,4 @@ void ultrasonicBegin(int trig, int echo);
 
 float readDistance(int trig, int echo);
 float computeLevel(float distance, float maxDist);
-float smoothLevel(float current, float previous);
+float smooth(float current, float previous, float alpha);
diff --git a/src/main.cpp b/src/main.cpp
index cade2f2..298e00c 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -1,8 +1,10 @@
+#include "config.h"
 #include "dht_sensor.h"
 #include "lcd_status.h"
 #include "led.h"
 #include "rate.h"
 #include "state.h"
+#include "state_data.h"
 #include "ultrasonic.h"
 #include <Arduino.h>
 
@@ -15,6 +17,12 @@
 float prevLevel = 0;
 unsigned long prevTime = 0;
 
+Config cfg = {.maxDistance = 80,
+              .smoothAlpha = 0.3,
+              .alertRate = 2.0,
+              .dangerLevel = 60,
+              .humidityThreshold = 70};
+
 void setup() {
   Serial.begin(115200);
 
@@ -27,29 +35,36 @@ void setup() {
   prevTime = millis();
 }
 
+SystemState sys = {0, 0};
+
 void loop() {
-  float distance = readDistance(TRIG, ECHO);
-  float level = computeLevel(distance, MAX_DIST);
-  float smooth = smoothLevel(level, prevLevel);
+  unsigned long now = millis();
+  float dt = (now - sys.prevTime) / 1000.0;
 
+  // --- INPUT ---
+  float distance = readDistance(TRIG, ECHO);
   float humidity = readHumidity();
   float temperature = readTemperature();
 
-  unsigned long now = millis();
-  float dt = (now - prevTime) / 1000.0;
+  // --- TRANSFORM ---
+  float level = computeLevel(distance, cfg.maxDistance);
+  float smoothLevel = smooth(level, sys.prevLevel, cfg.smoothAlpha);
+  float rate = computeRate(smoothLevel, sys.prevLevel, dt);
 
-  float rate = computeRate(smooth, prevLevel, dt);
-  State state = computeState(smooth, rate, humidity);
+  SensorData data = {distance, level, smoothLevel, humidity, temperature};
 
-  ledUpdate(state);
+  State state = computeState(data.smooth, rate, data.humidity, cfg);
 
-  lcdShowReadings(distance, level, smooth);
+  // --- OUTPUT (SIDE EFFECT) ---
+  ledUpdate(state);
+  lcdShowReadings(data.distance, data.level, data.smooth);
 
-  Serial.printf("L: %.2f | R: %.2f | H: %.2f | T: %.2f | %s\n", smooth, rate,
-                humidity, temperature, stateToString(state));
+  Serial.printf("L: %.2f | R: %.2f | H: %.2f | %s\n", data.smooth, rate,
+                data.humidity, stateToString(state));
 
-  prevLevel = smooth;
-  prevTime = now;
+  // --- UPDATE STATE ---
+  sys.prevLevel = data.smooth;
+  sys.prevTime = now;
 
   delay(600);
 }
diff --git a/src/state.cpp b/src/state.cpp
index b0ca401..fc23c5a 100644
--- a/src/state.cpp
+++ b/src/state.cpp
@@ -1,15 +1,18 @@
 #include "state.h"
+#include "config.h"
 #include <Arduino.h>
 
-State computeState(float level, float rate, float humidity) {
+State computeState(float level, float rate, float humidity, const Config &cfg) {
   if (isnan(humidity))
     humidity = 0;
 
-  if (level > 60)
+  if (level > cfg.dangerLevel)
     return State::DANGER;
-  if (rate > 2.0 && humidity > 70)
+
+  if (rate > cfg.alertRate && humidity > cfg.humidityThreshold)
     return State::ALERT;
-  if (rate > 3.5)
+
+  if (rate > cfg.alertRate * 1.5)
     return State::ALERT;
 
   return State::SAFE;
diff --git a/src/ultrasonic.cpp b/src/ultrasonic.cpp
index 9e0e278..58c5d0f 100644
--- a/src/ultrasonic.cpp
+++ b/src/ultrasonic.cpp
@@ -34,6 +34,6 @@ float computeLevel(float distance, float maxDist) {
   return level;
 }
 
-float smoothLevel(float current, float previous) {
-  return 0.7 * previous + 0.3 * current;
+float smooth(float current, float prev, float alpha) {
+  return (1 - alpha) * prev + alpha * current;
 }
